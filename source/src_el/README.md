# src_el

This directory is reserved for the future unresolved Euler-Lagrange coupling
subsystem.

Stage 0 decision:

- `source/src_fbm/` remains the home of resolved FBM coupling
- `source/src_el/` will hold unresolved Euler-Lagrange coupling code

Intended contents in later stages:

- `G2P` sampling helpers
- one-way E-L force closures
- `P2G` particle reaction-force spreading
- porosity or void-fraction accumulation
